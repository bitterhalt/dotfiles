#!/bin/sh
case $BLOCK_BUTTON in
	1) st -c floatterm -g 144x41 -e paru && pkill -RTMIN+1 dwmblocks ;;
    3) notify-send "UPDATES AVAILABLE:" "$(checkupdates | awk '{print $1}')" ;;
    6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac
cupd=$(checkupdates | wc -l)
. sb-theme
display " ^d^$cupd"



