#!/bin/sh

case $BLOCK_BUTTON in
	1) st -c floatterm -e pulsemixer ;;
	2) pactl set-sink-mute @DEFAULT_SINK@ toggle ;;
    6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac
. sb-theme
if [ "$(pamixer --get-mute)" = true ]; then
    display " "
    exit
fi

vol="$(pamixer --get-volume)"
[ $? -ne 0 ] && [ -z "$vol" ] && exit 1

case 1 in
	$((vol >= 30)) ) icon=" " ;;
	$((vol >= 15)) ) icon=" " ;;
	$((vol >= 1)) ) icon=" " ;;
	* ) display  && exit ;;
esac

display "$icon$vol%"
