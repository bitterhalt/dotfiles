#!/bin/bash

pactl set-source-mute @DEFAULT_SOURCE@ toggle

if pactl list sources | grep -q "Mute: yes"; then
    dunstify -t 3000 "Microphone is OFF"
else
    dunstify -t 3000 "Microphone is ON"
fi
pkill -RTMIN+7 dwmblocks


