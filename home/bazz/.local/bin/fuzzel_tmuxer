#!/usr/bin/env bash

tmux list-sessions -F "#{session_name}" 2>/dev/null |
  fuzzel -d -a top --y 6 --x 6 -w 25 --minimal-lines --placeholder "Choose session..." |
  xargs -I{} "$TERMINAL" -e tmux attach-session -t {}
