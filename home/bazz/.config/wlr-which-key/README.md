## ⚠️ NOTE for editing wlr-which-key menus

> If you wan't to edit these use template files in ~/.config/wal/templates/ or else these gets rewritten. Sucks but it's the only way to support Pywal since wlr-which-key can't source config files.
> After editing reload Pywal by setting theme or walpaper.
